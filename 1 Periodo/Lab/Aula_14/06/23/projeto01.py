from os import 

secreta = 'SISTEMAS'
secreta = secreta.upper()
palavra = '=' * len(secreta)
vidas = 10
digitadas = ' '
print (palavra)
letra = input('Digite a primeira letra: ').upper()
if digitadas.find(letra) >= 0:

else:
    digitadas += letra

for i in range(len(secreta)):
    if letra == secreta[i]:
        auxiliar

vidas -= 1